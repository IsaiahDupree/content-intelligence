/** @type {import('next').NextConfig} */
const nextConfig = {
  // This is a Python/Flask backend, not a Next.js app
  // But keeping this file for reference if frontend is added later
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = nextConfig
